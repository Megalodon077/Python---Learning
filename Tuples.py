coordinates = (4,5)  #Just like list but cant modify it   "()"

print(coordinates[0])  # can access the elements inside but cant change it

coordinates = [(4,5),(6,4),(3,4)]    #can add tupples in a list



#If and else

